name: Build and publish application

on:
  push:
    branches: [main]
    tags: ['*']

jobs:
  build-publish:
    name: Build, Push, Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout app repo
        uses: actions/checkout@v6

      - name: Checkout config repo
        uses: actions/checkout@v6
        with:
          repository: lindkon/dwk-config
          token: ${{ secrets.CONFIG_REPO_TOKEN }} 
          path: config

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and publish images
        run: |-
          docker build --tag "lindkon/broadcaster:$GITHUB_SHA" the_project/broadcaster
          docker build --tag "lindkon/todo-backend:$GITHUB_SHA" the_project/todo-backend
          docker build --tag "lindkon/todo-main:$GITHUB_SHA" the_project/todo-app/todo-main
          docker build --tag "lindkon/get-image:$GITHUB_SHA" the_project/todo-app/get-image
          docker push "lindkon/broadcaster:$GITHUB_SHA"
          docker push "lindkon/todo-backend:$GITHUB_SHA"
          docker push "lindkon/todo-main:$GITHUB_SHA"
          docker push "lindkon/get-image:$GITHUB_SHA"

      - name: Set up Kustomize
        uses: imranismail/setup-kustomize@v3

      - name: Update staging images
        if: startsWith(github.ref, 'refs/heads/')    
        working-directory: config/overlays/staging
        run: |-
          kustomize edit set image lindkon/broadcaster=*:$GITHUB_SHA
          kustomize edit set image lindkon/todo-backend=*:$GITHUB_SHA
          kustomize edit set image lindkon/todo-main=*:$GITHUB_SHA
          kustomize edit set image lindkon/get-image=*:$GITHUB_SHA

      - name: Update production images
        if: startsWith(github.ref, 'refs/tags/')    
        working-directory: config/overlays/prod
        run: |-
          kustomize edit set image lindkon/broadcaster=*:$GITHUB_SHA
          kustomize edit set image lindkon/todo-backend=*:$GITHUB_SHA
          kustomize edit set image lindkon/todo-main=*:$GITHUB_SHA
          kustomize edit set image lindkon/get-image=*:$GITHUB_SHA

      - name: Commit kustomization to config repo
        uses: EndBug/add-and-commit@v10
        with:
          cwd: config                                
          add: 'overlays/*/kustomization.yaml'        
          message: 'New version released ${{ github.sha }}'